# csh version; use setup if you're in bash
# Environment variables that need to be set before running the app
setenv FLASK_APP "rethink.py"
source env/bin/activate.csh # this assumes you've set up your virtual environment in the 'env' directory
